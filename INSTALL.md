# Support Portal — Инструкция по установке

## Описание системы

**Support Portal** — веб-приложение для управления клиентами (аптеками), провайдерами и сетевой инфраструктурой.

### Стек технологий

| Компонент | Технология | Назначение |
|-----------|-----------|------------|
| Backend | Django 4.x + Django REST Framework | REST API, бизнес-логика, аутентификация |
| Frontend | React 18 + Ant Design | Веб-интерфейс |
| База данных | PostgreSQL 16 | Хранение данных |
| Веб-сервер | Nginx (Alpine) | Проксирование, раздача статики и медиафайлов |
| Контейнеризация | Docker + Docker Compose | Изоляция и управление сервисами |

### Основные модули и функции

**Клиенты**
- Карточка клиента: адрес, компания, ИНН, телефон, email, код аптеки, ICCID
- Провайдер: название, тип подключения (⚡ Оптоволокно, ☎️ DSL, 🔌 Кабель, 📡 Беспроводное, 📶 Модем, ↔️ MR-Net), тариф, лицевой счёт, № договора, настройки, оборудование провайдера
- При выборе типа Модем или MR-Net: дополнительные поля Номер (модем/SIM) и ICCID модема
- Передача модема между клиентами с логированием
- Сеть: подсеть аптеки, внешний IP (кликабельная ссылка), Микротик IP (кликабельная ссылка, авто: .1), Сервер IP (авто: .2)
- Проверка доступности IP-адресов (ping)
- Получение внешнего IP с Микротика по SSH
- Заметки и история изменений (последние 2 записи видны сразу, остальные сворачиваются)
- Вложенные файлы (до 5 МБ на файл): превью для изображений с галереей, скачивание
- Система черновиков при создании клиента
- Экспорт списка клиентов в Excel с учётом активных фильтров

**Пользователи и роли**
- RBAC: роли с настраиваемыми правами
- Права: просмотр, создание, редактирование, удаление клиентов, управление пользователями, ролями, полями

**Провайдеры**
- Справочник провайдеров для выбора в карточке клиента

**Настройки системы** (только для администраторов)
- SSH-параметры для подключения к Микротикам (логин, пароль хранится зашифрованным через Fernet)
- Кнопка очистки SSH-данных

---

## Требования к серверу

- Ubuntu 20.04 / 22.04 / 24.04
- 2 ГБ RAM минимум (рекомендуется 4 ГБ)
- 20 ГБ диска
- Открытый порт 80 (и 443 при HTTPS)

---

## 1. Подготовка сервера

### 1.1 Обновление системы

```bash
apt update && apt upgrade -y
```

### 1.2 Установка Docker

Docker используется для запуска всех сервисов (backend, frontend, база данных, nginx) в изолированных контейнерах.

```bash
curl -fsSL https://get.docker.com | sh
```

### 1.3 Установка Docker Compose Plugin

Docker Compose позволяет управлять всеми контейнерами одной командой через файл `docker-compose.yml`.

```bash
apt install docker-compose-plugin -y
```

### 1.4 Установка Git

Git нужен для клонирования репозитория и получения обновлений проекта.

```bash
apt install git -y
```

### 1.5 Установка Python 3 и pip

Python нужен для генерации ключей шифрования и запуска вспомогательных скриптов на сервере вне контейнера.

```bash
apt install python3 python3-pip -y
```

### 1.6 Установка библиотеки cryptography

Используется для генерации `ENCRYPTION_KEY` — Fernet-ключа, которым шифруется SSH-пароль в базе данных.

```bash
pip3 install cryptography --break-system-packages
```

### 1.7 Установка системных утилит

```bash
# curl — HTTP-запросы, скачивание файлов и скриптов
apt install curl -y

# nano — текстовый редактор для редактирования .env и конфигов
apt install nano -y

# iputils-ping — утилита ping, используется бэкендом для проверки доступности IP
apt install iputils-ping -y

# openssl — генерация SECRET_KEY для Django
apt install openssl -y
```

### 1.8 Проверка установки

После установки всех компонентов убедитесь что всё работает:

```bash
docker --version          # Docker version 24.x.x или выше
docker compose version    # Docker Compose version v2.x.x или выше
git --version             # git version 2.x.x
python3 --version         # Python 3.x.x
pip3 --version            # pip 23.x.x
curl --version            # curl 7.x.x
ping -c1 8.8.8.8          # должен пройти пинг
```

### 1.9 Добавление пользователя в группу docker (опционально)

Если вы работаете не от root, добавьте своего пользователя в группу docker чтобы не писать `sudo` перед каждой командой:

```bash
usermod -aG docker $USER
newgrp docker
```

---

## 2. Загружаем проект

```bash
git clone https://github.com/suminslava55-droid/support-portal-new.git /opt/support-portal
cd /opt/support-portal
```

---

## 3. Настраиваем переменные окружения

```bash
cp .env.example .env
nano .env
```

Содержимое `.env`:

```env
# База данных
DB_NAME=support_portal
DB_USER=postgres
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_HOST=db
DB_PORT=5432

# Django
SECRET_KEY=CHANGE_ME_VERY_LONG_SECRET_KEY_HERE
DEBUG=False
ALLOWED_HOSTS=your-ip-or-domain,localhost
CORS_ALLOWED_ORIGINS=http://your-ip-or-domain,http://localhost

# Шифрование SSH-пароля в базе данных
ENCRYPTION_KEY=CHANGE_ME_FERNET_KEY
```

### Генерация значений

**SECRET_KEY:**
```bash
openssl rand -hex 50
```

**ENCRYPTION_KEY** (Fernet-ключ для шифрования SSH-пароля):
```bash
python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

**ALLOWED_HOSTS и CORS_ALLOWED_ORIGINS:**
```env
# Только IP
ALLOWED_HOSTS=94.130.1.1,localhost
CORS_ALLOWED_ORIGINS=http://94.130.1.1,http://localhost

# С доменом
ALLOWED_HOSTS=support.mycompany.ru,localhost
CORS_ALLOWED_ORIGINS=https://support.mycompany.ru,http://localhost
```

### Итоговый пример заполненного .env

```env
DB_NAME=support_portal
DB_USER=postgres
DB_PASSWORD=qX7!mN3kP9@support
DB_HOST=db
DB_PORT=5432

SECRET_KEY=a3f8c2d1e4b7a9f0c3d6e2b5a8f1c4d7e0b3a6f9c2d5e8b1a4f7c0d3e6b9a2f5c8d1e4b7
DEBUG=False
ALLOWED_HOSTS=94.130.1.1,localhost
CORS_ALLOWED_ORIGINS=http://94.130.1.1,http://localhost

ENCRYPTION_KEY=ySKoI9K2_Uj2qjNUSxREYTFIW3o-HILiIMYZ5enMgYs=
```

> **Сохранение в nano:** `Ctrl+O` → `Enter` → `Ctrl+X`

---

## 4. Хранилище медиафайлов

Вложения клиентов хранятся в папке `./media` на хосте (bind mount). При перезапуске контейнеров файлы сохраняются.

```bash
mkdir -p /opt/support-portal/media
```

Структура:
```
media/
└── clients/
    ├── 1/      ← файлы клиента ID=1
    ├── 2/
    └── ...
```

---

## 5. Запускаем

```bash
cd /opt/support-portal
docker compose up -d --build
```

Первый запуск занимает 5–10 минут. Следим за процессом:

```bash
docker compose logs -f
```

Проверяем что все контейнеры запустились:

```bash
docker compose ps
```

Должны быть запущены: `support-portal-db-1`, `support-portal-backend-1`, `support-portal-nginx-1`.

---

## 6. Создаём администратора и роли

```bash
docker compose exec backend python create_admin.py
```

Скрипт создаст:
- Роль **Администратор** со всеми правами
- Роль **Техник** с правами просмотра и редактирования
- Первого пользователя-администратора (введите email, пароль, имя)

---

## 7. Первоначальная настройка в интерфейсе

1. Откройте `http://ВАШ_IP` в браузере
2. Войдите под учётными данными администратора
3. Перейдите в **Настройки** (левое меню, видно только администраторам):
   - Укажите **SSH пользователь** и **SSH пароль** для Микротиков
   - Кнопка **Очистить** удаляет SSH-данные из базы
4. Перейдите в **Провайдеры** и добавьте провайдеров
5. Начните создавать карточки клиентов

---

## 8. Функция получения внешнего IP с Микротика

В карточке клиента рядом с полем **Внешний IP** есть кнопка **Получить**. Алгоритм работы:

1. Проверяется поле **Подсеть аптеки** — из него вычисляется Микротик IP (последний октет `.1`)
2. Проверяется наличие SSH-параметров в настройках системы
3. Выполняется ping Микротика
4. Устанавливается SSH-соединение
5. На Микротике выполняется команда:
   ```
   :put ([/tool fetch url="https://api.ipify.org" http-method=get output=user as-value]->"data")
   ```
6. IP подставляется в поле. Выводится уведомление: получен впервые / изменился (старый → новый) / не изменился

**Требования к Микротику:**
- SSH порт 22 открыт и доступен
- Доступ в интернет (для запроса к `api.ipify.org`)
- Пользователь с правами выполнения команд

---

## 9. Передача модема между клиентами

При редактировании клиента с типом подключения **Модем** или **MR-Net** появляется кнопка **Передать** рядом с полем "Тип подключения". При передаче:
- У клиента-получателя перезаписываются: тип подключения, номер модема, ICCID модема
- У клиента-отправителя тип подключения очищается
- В истории изменений обоих клиентов фиксируется событие передачи

---

## 10. Экспорт клиентов в Excel

На странице списка клиентов в строке фильтров есть кнопка с иконкой Excel. Экспортируются только те клиенты, которые соответствуют активным фильтрам (поиск, статус). Файл содержит все поля карточки клиента с форматированием и автофильтром.

---

## Полезные команды

```bash
# Статус контейнеров
docker compose ps

# Логи в реальном времени
docker compose logs -f backend
docker compose logs -f nginx

# Перезапустить бэкенд (после изменений Python-кода)
docker compose restart backend

# Полная пересборка (после изменений зависимостей или фронтенда)
docker compose up -d --build

# Применить новые миграции
docker compose exec backend python manage.py migrate

# Создать миграции после изменений в models.py
docker compose exec backend python manage.py makemigrations

# Список миграций
docker compose exec backend python manage.py showmigrations

# Резервная копия БД
docker compose exec db pg_dump -U postgres support_portal > backup_$(date +%Y%m%d).sql

# Восстановление из резервной копии
cat backup_20240101.sql | docker compose exec -T db psql -U postgres support_portal
```

---

## Обновление системы

```bash
cd /opt/support-portal
git pull
docker compose up -d --build
docker compose exec backend python manage.py migrate
```

---

## Устранение неполадок

**Белый экран в браузере**
```bash
docker compose logs nginx --tail=30
# Также откройте F12 → Console в браузере для деталей ошибки JS
```

**Ошибка 500 от API**
```bash
docker compose logs backend --tail=50
```

**Не загружаются медиафайлы (404 на /media/)**
```bash
ls -la /opt/support-portal/media/
docker compose exec nginx nginx -t
```

**TLS timeout при сборке (нет интернета)**
```bash
# Образы уже собраны — запускаем без пересборки
docker compose up -d
```

**Черновики клиентов накапливаются в базе**
```bash
docker compose exec backend python manage.py shell -c "
from apps.clients.models import Client
from django.utils import timezone
from datetime import timedelta
old = Client.objects.filter(is_draft=True, created_at__lt=timezone.now()-timedelta(days=1))
print(f'Найдено черновиков: {old.count()}')
for c in old:
    for f in c.files.all():
        f.file.delete()
        f.delete()
    c.delete()
print('Готово')
"
```

**После смены ENCRYPTION_KEY SSH-пароль не читается**

При смене ключа шифрования старый пароль в базе становится нечитаемым. Нужно заново ввести SSH-пароль в разделе **Настройки**.

---

## Безопасность

Файл `.env` содержит секретные ключи и **не должен попадать в git**. Убедитесь что `.gitignore` содержит:

```
.env
media/
*.pyc
__pycache__/
```

При компрометации ключей сгенерируйте новые:
```bash
# Новый SECRET_KEY
openssl rand -hex 50

# Новый ENCRYPTION_KEY
python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

Обновите `.env` и перезапустите бэкенд:
```bash
docker compose restart backend
```

---

## Настройка HTTPS (если есть домен)

```bash
apt install certbot python3-certbot-nginx -y
docker compose stop nginx
certbot certonly --standalone -d ваш-домен.ru

# Добавьте в nginx/default.conf:
# server {
#   listen 443 ssl;
#   ssl_certificate /etc/letsencrypt/live/ваш-домен.ru/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/ваш-домен.ru/privkey.pem;
# }

docker compose start nginx
```

---

## Структура проекта

```
support-portal/
├── backend/
│   ├── apps/
│   │   ├── accounts/             # Пользователи, роли, JWT
│   │   └── clients/
│   │       ├── models.py         # Client, Provider, ClientFile, SystemSettings
│   │       ├── views.py          # API, SSH, экспорт Excel, передача модема
│   │       ├── serializers.py
│   │       ├── settings_views.py # Настройки системы (SSH)
│   │       └── migrations/
│   ├── config/
│   │   ├── settings.py
│   │   └── urls.py
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   ├── src/
│   │   ├── pages/
│   │   │   ├── ClientsPage.jsx       # Список + экспорт Excel
│   │   │   ├── ClientDetailPage.jsx  # Карточка: превью фото, кликабельные IP
│   │   │   ├── ClientFormPage.jsx    # Создание/редактирование + передача модема
│   │   │   ├── SettingsPage.jsx      # SSH настройки
│   │   │   ├── UsersPage.jsx
│   │   │   └── ProvidersPage.jsx
│   │   ├── components/
│   │   │   └── AppLayout.jsx
│   │   └── api/index.js
│   └── Dockerfile
├── nginx/
│   └── default.conf
├── media/                        # Медиафайлы (bind mount, хранится на хосте)
├── docker-compose.yml
├── .env                          # Не в git!
└── .env.example
```

---

## Python-зависимости (requirements.txt)

| Пакет | Назначение |
|-------|-----------|
| django | Основной веб-фреймворк |
| djangorestframework | REST API |
| djangorestframework-simplejwt | JWT аутентификация |
| psycopg2-binary | Драйвер PostgreSQL |
| django-cors-headers | CORS заголовки для фронтенда |
| django-filter | Фильтрация в API |
| python-dotenv | Загрузка .env файла |
| cryptography | Шифрование SSH-пароля (Fernet) |
| paramiko | SSH-подключение к Микротику |
| openpyxl | Генерация Excel файлов для экспорта |
| gunicorn | WSGI-сервер для продакшена |
| Pillow | Обработка изображений |