# Support Portal — Справочник

## Модули системы

### 📋 Клиенты
- Карточка клиента: адрес, компания, ИНН, телефон, email, код аптеки (UT), код склада, ICCID
- Три вкладки: **📋 Информация**, **🌐 Провайдеры**, **🧾 ККТ**
- Блок **Сеть**: подсеть, внешний IP, Микротик IP (авто: .1), Сервер IP (авто: .2), проверка доступности
- **Провайдер 1 и 2**: название, тип подключения, тариф, лицевой счёт, № договора, настройки, оборудование
- Передача провайдера между клиентами с выбором слота и логированием
- Получение внешнего IP с Микротика по SSH (paramiko)
- Заметки и история изменений с иконками
- Вложенные файлы до 5 МБ
- Система черновиков (старше 2 часов удаляются автоматически)
- Экспорт списка в Excel с выбором полей и отправкой на Email
- Фильтрация по нескольким провайдерам одновременно
- Поиск по: адресу, телефону, email, коду аптеки, коду склада, компании ОФД, ИНН, лицевому счёту, № договора, типу подключения

### 🧾 ККТ (Кассовая техника)
- Раздел **«Компании»** — справочник с ИНН и зашифрованным токеном ОФД (lk.ofd.ru)
- Вкладка **«ККТ»** в карточке клиента — просмотр, получение и обновление данных
- Поля: РНМ, серийный номер, модель, номер ФН, дата создания, конец срока ФН, конец договора ОФД, адрес установки
- Проверка совпадения адреса при добавлении по РНМ

### 🔄 Замена ФН
- **Вкладка Общий** — полный список всех ККТ с поиском, сортировкой, экспортом Excel
- **Вкладка По месяцам** — фильтр по месяцу и году окончания срока ФН
- Подсветка дат: красная (просрочен), оранжевая (≤30 дней), жёлтая (≤90 дней)
- Клик по строке — переход в карточку клиента на вкладку ККТ

### 👤 Пользователи и роли
- RBAC: роли с настраиваемыми правами
- Роли: **Администратор**, **Системный администратор**, **Связист**
- Поле **День рождения** — отображается в календаре дежурств

### 📅 Календарь дежурств
- Типы дежурств: Телефон, Работа днём, Телефон + день, Отпуск, Занят
- Выходные дни администратор может настраивать вручную
- Мультивыделение: Ctrl+клик для массового назначения типа
- Кнопка **Отчёт за месяц** — сводная таблица

### ⚙️ Настройки (только Администратор)
- SSH-параметры для Микротиков (пароль шифруется через Fernet)
- SMTP-параметры для Email (пароль шифруется через Fernet)
- Тестовая отправка письма

---

## Архитектура ОФД

```
Браузер → Django API (контейнер) → ofd_fetch.sh (хост) → lk.ofd.ru
```

Скрипт `ofd_fetch.sh` принимает аргументы:
```bash
# Поиск по адресу (полный)
python3 /opt/support-portal/ofd_fetch.sh <ИНН> <ТОКЕН> "Город, Улица, д. N"

# Прямой запрос по РНМ (быстрый)
python3 /opt/support-portal/ofd_fetch.sh <ИНН> <ТОКЕН> 0001234567890123
```

### Разница между кнопками получения ККТ

| Кнопка | Метод | Что делает |
|--------|-------|-----------|
| Получить данные ККТ по ИНН | POST | Полный поиск по ИНН + фильтрация по адресу. Находит новые ККТ |
| Обновить по РНМ | PATCH | Обновляет только уже сохранённые ККТ по их РНМ. Быстрее |
| По РНМ (при создании) | PATCH | Получает данные по введённым вручную РНМ. Проверяет совпадение адреса |

---

## Структура проекта

```
support-portal/
├── backend/
│   ├── apps/
│   │   ├── accounts/
│   │   │   ├── accounts_views.py     # Смена пароля, профиль
│   │   │   ├── admin.py
│   │   │   ├── management/
│   │   │   │   └── commands/
│   │   │   │       └── init_roles.py # Команда инициализации ролей
│   │   │   ├── models.py             # User (+ birthday), Role
│   │   │   ├── permissions.py        # IsAdmin, CanEditClient и др.
│   │   │   ├── serializers.py
│   │   │   ├── urls.py
│   │   │   ├── views.py
│   │   │   └── migrations/
│   │   └── clients/
│   │       ├── admin.py
│   │       ├── models.py             # Client, Provider, ClientFile, SystemSettings,
│   │       │                         # DutySchedule, CustomHoliday, OfdCompany, KktData
│   │       ├── views.py              # API клиентов, SSH, Excel, ККТ, замена ФН
│   │       ├── serializers.py
│   │       ├── settings_views.py     # Настройки SSH и SMTP
│   │       ├── urls.py
│   │       └── migrations/           # включая 0024_kktdata, 0025_ofdcompany,
│   │                                 # 0026_warehouse_code
│   ├── config/
│   │   ├── settings.py
│   │   ├── urls.py
│   │   └── wsgi.py
│   ├── requirements.txt
│   ├── create_admin.py               # Создание администратора и ролей
│   ├── manage.py
│   └── Dockerfile
├── frontend/
│   ├── src/
│   │   ├── App.jsx                   # Роутинг, тёмная тема
│   │   ├── main.jsx
│   │   ├── index.js
│   │   ├── api/
│   │   │   ├── index.js              # Экспорт всех API
│   │   │   ├── axios.js              # Настройка axios, JWT interceptor
│   │   │   ├── auth.js               # Авторизация
│   │   │   ├── clients.js            # API клиентов
│   │   │   └── users.js              # API пользователей
│   │   ├── components/
│   │   │   ├── AppLayout.jsx         # Боковое меню, пункты по ролям
│   │   │   ├── MainLayout.jsx        # Общий layout страницы
│   │   │   └── ProtectedRoute.js     # Защита маршрутов
│   │   ├── pages/
│   │   │   ├── ClientsPage.jsx       # Список клиентов, фильтры, экспорт
│   │   │   ├── ClientDetailPage.jsx  # Просмотр карточки, вкладка ККТ
│   │   │   ├── ClientFormPage.jsx    # Создание/редактирование карточки
│   │   │   ├── OfdCompaniesPage.jsx  # Управление компаниями ОФД
│   │   │   ├── FnReplacementPage.jsx # Замена ФН: Общий и По месяцам
│   │   │   ├── CalendarPage.jsx      # Календарь дежурств
│   │   │   ├── SettingsPage.jsx      # Настройки SSH и SMTP
│   │   │   ├── UsersPage.jsx         # Управление пользователями
│   │   │   ├── ProvidersPage.jsx     # Справочник провайдеров
│   │   │   ├── LoginPage.jsx         # Страница входа
│   │   │   └── CustomFieldsPage.jsx  # Пользовательские поля
│   │   └── store/
│   │       ├── authStore.js          # Zustand: авторизация, права
│   │       └── themeStore.js         # Zustand: тёмная тема
│   ├── public/
│   │   ├── index.html
│   │   └── saint.jpg
│   ├── build/                        # Собранный фронтенд (не в git)
│   ├── package.json
│   ├── package-lock.json
│   ├── vite.config.js
│   ├── index.html
│   ├── nginx.conf                    # Nginx конфиг для Dockerfile фронтенда
│   └── Dockerfile
├── nginx/
│   ├── default.conf                  # Основной конфиг Nginx (проксирование API)
│   └── nginx.conf
├── media/                            # Медиафайлы клиентов (не в git)
├── ofd_fetch.sh                      # Скрипт запросов к ОФД (на хосте)
├── check_ofd_network.sh              # Диагностика сети до lk.ofd.ru
├── check_ofd_tokens.sh               # Проверка токенов всех компаний
├── diagnose.sh                       # Универсальная диагностика 500/400 ошибок
├── deploy-frontend.sh                # Быстрый деплой фронтенда без Docker
├── docker-compose.yml
├── .env                              # Не в git!
├── .env.example
└── .gitignore
```

---

## Python-зависимости (requirements.txt)

| Пакет | Версия | Назначение |
|-------|--------|-----------|
| Django | 4.2.9 | Основной веб-фреймворк |
| djangorestframework | 3.14.0 | REST API |
| djangorestframework-simplejwt | 5.3.1 | JWT аутентификация |
| django-cors-headers | 4.3.1 | CORS заголовки для фронтенда |
| psycopg2-binary | 2.9.9 | Драйвер PostgreSQL |
| python-dotenv | 1.0.0 | Загрузка .env файла |
| Pillow | 10.2.0 | Обработка изображений |
| django-filter | 23.5 | Фильтрация в API |
| cryptography | — | Шифрование паролей SSH, SMTP и токенов ОФД (Fernet) |
| paramiko | — | SSH-подключение к Микротику |
| openpyxl | — | Генерация Excel файлов |
| gunicorn | — | WSGI-сервер для продакшена |
